type Mutation {
  login(
    username: String!
    password: String!
    appId: uuid!
  ): login_response
}

type Mutation {
  validate_token: validate_token_response
}

type login_response {
  token: String!
  userId: uuid!
}

type validate_token_response {
  userId: uuid!
}

