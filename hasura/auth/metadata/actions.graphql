type Query {
  sign_in(
    username: String!
    password: String!
    app_id: uuid!
  ): sign_in_response
}

type Query {
  validate_token: validate_token_response
}

type sign_in_response {
  token: String!
  user_id: uuid!
}

type validate_token_response {
  user_id: uuid!
}

