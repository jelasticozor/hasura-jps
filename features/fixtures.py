from behave import fixture, use_fixture
from behave.fixture import use_composite_fixture_with, fixture_call_params


# @fixture
# def random_seed(context):
#     random.seed('jelasticozor-infrastructure-tests')


# @fixture
# def api_client(context):
# userdata = context.config.userdata
# hasura_endpoint = userdata['hasura-endpoint']
# hasura_admin_secret = userdata['admin-secret']
# context.api_client = ApiClient(
# hasura_endpoint, hasura_admin_secret, graphql_calls_folder)
# return context.api_client


fixtures_registry = {

}
